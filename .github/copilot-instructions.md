# Copilot Instructions

## Project Guidelines
- User prefers refactoring Program.cs to keep startup code concise by extracting service registration and middleware pipeline into extension methods.
- User prefers OpenAPI request examples to be declared close to the request DTO/record, using an attribute-based approach instead of centralized if/else mapping.